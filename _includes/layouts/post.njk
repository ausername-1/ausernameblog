---
 layout: layouts/base.njk
---

<h2>{{ title }}</h2>
<div id="publisheddate" class="subtitle text-muted mb-2">Published on {{ date | readableDate }}</div>
<img src="{{ featuredImage | url }}" alt="{{title}}"></img>
<p>{{ content | safe}}</p>
